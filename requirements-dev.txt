-r requirements.txt

pytest>=9.1,<10            # test runner
pytest-cov>=7.1,<8         # coverage measurement, --cov-fail-under=80 gate
freezegun>=1.5,<2          # deterministic timestamps in metrics tests
ruff>=0.16                 # lint — catches unused imports etc. before review
