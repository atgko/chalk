"""Streamlit view-render helpers imported by app.py.

These modules render UI and call into the chalk package for all business
logic — they should not contain business logic themselves (PLAN.md Risk #8).
"""
